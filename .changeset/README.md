# Changesets

Add one changeset for each user-visible release change. This private Tauri app
uses Changesets for version and changelog preparation; GitHub Actions publishes
the signed desktop assets.
